# 📚 EPUB2PDF 转换器 (v3.4.0)

一个基于 Python 开发的高效电子书转换工具，专为处理大型古籍（如《二十四史》）及复杂排版设计。

## ✨ 核心特性 (Key Features)

相较于普通的转换工具，本项目针对**大体量书籍**（如《二十四史》）和**复杂排版**进行了深度优化：

* **🛡️ 智能分卷策略 (Smart Splitting Strategy)**
    自动检测超大文件（>20MB），采用“拆分-转换-合并”的流式处理机制，有效解决内存溢出与转换卡死问题，大部头古籍也能丝滑转换。

* **📑 目录层级重构 (Hierarchical TOC Reconstruction)**
    告别混乱的合并目录。在合并分卷时，算法会自动将“书名/文件名”提升为**一级目录**，原书目录自动降级，完美保留多书合集的层级结构。

* **🎨 完美排版还原 (Perfect Layout Rendering)**
    * **注释角标化**：智能识别文中的“注”字链接与图标，自动转换为右上角**微型角标**，不再撑大行高，阅读体验对齐原生 PDF。
    * **全局图片索引**：内置 Image Manifest 系统，无视 EPUB 内部复杂的相对路径，通过文件名哈希精准定位图片，彻底修复图片丢失与“alt”占位符乱码问题。

* **⚡ 全自动工作流 (Automated Workflow)**
    一键实现 `解压 -> 清洗 -> 渲染 -> 合并 -> 清理` 全流程闭环。支持实时进度监控、耗时统计及临时文件自动管理。

## 🛠️ 安装与运行

1. 克隆项目：
   ```bash
   git clone [https://github.com/Gramaryeli/EPUB2PDF.git](https://github.com/Gramaryeli/EPUB2PDF.git)
   ```
2. 安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
3. 运行：
   ```bash
   python APP.py
   ```

## 📦 下载使用

普通用户无需配置 Python 环境，请直接前往 [Releases 页面](https://github.com/Gramaryeli/EPUB2PDF/releases) 下载最新 Windows 可执行文件 (.exe)。